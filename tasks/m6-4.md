# M6-4: UI改善（OnGUI → uGUI移行）

読むファイル: BattleHUD.cs, MinimapHUD.cs, ScoreboardHUD.cs, EnhancementRingHUD.cs
変更ファイル: 全HUDファイル

OnGUIベースのUIをuGUI（Canvas/Image/Text）に移行する。

1. 全HUDの移行:
   - BattleHUD → Canvas + Slider(HP) + Slider(無双ゲージ) + Image(ターゲットHP)
   - MinimapHUD → RenderTexture + Minimapカメラ方式に変更
   - ScoreboardHUD → Canvas + Text(タイマー/スコア)
   - EnhancementRingHUD → Canvas + 回転UIアニメーション
   - LobbyHUD → Canvas + ボタン + 入力フィールド

2. 追加UI画面:
   - タイトル画面
   - 武器選択画面（武器1/武器2、刻印選択）
   - 鍛錬画面（ステータス振り分けスライダー）
   - 設定画面（音量、画質、キーバインド）
   - リザルト画面（MVP、個人戦績）

3. UIデザイン:
   - 三國志風のテーマ（赤/金/黒基調）
   - フォント: 明朝体系

git commit -m "M6-4: UI改善（uGUI移行＆追加画面）"
