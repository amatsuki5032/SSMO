# M5-6: サーバーログ＆監視

読むファイル: AntiCheat.cs, GameModeManager.cs, GameConfig.cs
変更ファイル: 新規 Scripts/Server/ServerLogger.cs, GameConfig.cs

サーバー側のイベントログと監視システムを実装する。

1. 新規 Scripts/Server/ServerLogger.cs：
   - MonoBehaviour（サーバー専用、シングルトン）
   - 重要イベントのログ記録:
     - プレイヤー接続/切断
     - 撃破/被撃破
     - チート検出（AntiCheatからの通知）
     - ゲーム開始/終了/結果
     - エラー/例外
   - ログ出力先: ファイル（ローカル）+ Firestore（将来）
   - ログレベル: Info / Warning / Error
   - タイムスタンプ付き

2. 戦闘ログ:
   - 各撃破のログ（攻撃者、被弾者、使用技、ダメージ量）
   - 試合結果サマリー（チームスコア、MVP等）

3. GameConfig.cs に追加：
   - LOG_FILE_PATH = "server_logs/"
   - LOG_RETENTION_DAYS = 7

git commit -m "M5-6: サーバーログ＆監視"
