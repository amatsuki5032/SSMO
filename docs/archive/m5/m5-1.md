# M5-1: Firebase Auth 認証

読むファイル: GameConfig.cs, CLAUDE.md
変更ファイル: GameConfig.cs, 新規 Scripts/Netcode/AuthManager.cs

Firebase Authenticationによるプレイヤー認証を実装する。

## 実装内容

1. 新規 Scripts/Netcode/AuthManager.cs：
   - MonoBehaviour（シングルトン）
   - Firebase Auth SDK初期化
   - 匿名認証（Anonymous Auth）でまず実装（メール/パスワードは後）
   - ログイン成功時にUIDを保持（public string CurrentUid）
   - NetworkManager接続前にAuth完了を必須にする
   - 認証状態変更のイベント（event Action<bool> OnAuthStateChanged）
   - 再認証・トークンリフレッシュ処理

2. HelloNetwork.cs 修正：
   - Host/Client ボタン押下前に AuthManager.CurrentUid が null でないことを確認
   - 未認証なら「認証中...」表示

3. サーバー側検証：
   - NetworkManager.ConnectionApprovalCallback でクライアント接続時にUID検証
   - 不正UIDは接続拒否

4. GameConfig.cs に追加：
   - AUTH_TIMEOUT = 10f（認証タイムアウト秒数）

## Unity手動設定 🔧
- Firebase SDK for Unity をインポート（firebase_unity_sdk.zip）
- Assets/ に google-services.json を配置
- Firebase Console でプロジェクト作成 → Anonymous Auth を有効化
- Player Settings → Other Settings → Scripting Backend: IL2CPP（Firebase推奨）

git commit -m "M5-1: Firebase Auth認証基盤"
