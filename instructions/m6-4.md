# M6-4: UI改善（OnGUI → uGUI移行）

読むファイル: BattleHUD.cs, MinimapHUD.cs, ScoreboardHUD.cs, EnhancementRingHUD.cs, NetworkStatsHUD.cs, HelloNetwork.cs
変更ファイル: 上記全ファイル

現在のOnGUIベースUIをuGUI（Canvas + Image + Text）に移行する。

## 実装内容

### 1. Canvas構成

```
Canvas (Screen Space - Overlay)
├── BattlePanel
│   ├── HPBar (Image + Slider)
│   ├── MusouGaugeBar (Image + Slider)
│   └── TargetPanel
│       ├── TargetName (TextMeshPro)
│       └── TargetHPBar (Image + Slider)
├── ScoreboardPanel
│   ├── Timer (TextMeshPro)
│   ├── RedScore (TextMeshPro)
│   └── BlueScore (TextMeshPro)
├── MinimapPanel
│   ├── MinimapImage (RawImage + RenderTexture)
│   └── PlayerDots (Image × n)
├── EnhancementRingPanel
│   ├── SentanCount (TextMeshPro)
│   └── SlotIcons (Image × 7)
├── GameOverPanel (非表示→試合終了時表示)
│   └── ResultText (TextMeshPro)
└── DebugPanel (#if UNITY_EDITOR)
    ├── NetworkStats (TextMeshPro)
    └── DebugCommands (TextMeshPro)
```

### 2. 各HUD修正

| ファイル | 変更内容 |
|---------|---------|
| BattleHUD.cs | OnGUI → Canvas参照に書き換え。HPバーはImage.fillAmount、色変化はColor.Lerp |
| MinimapHUD.cs | OnGUI → RawImage + ミニマップカメラ（RenderTexture） |
| ScoreboardHUD.cs | OnGUI → TextMeshPro参照 |
| EnhancementRingHUD.cs | OnGUI → Canvas参照 |
| NetworkStatsHUD.cs | OnGUI → Canvas参照（#if UNITY_EDITOR） |
| HelloNetwork.cs | OnGUI → Canvas参照（ロビーUI） |

### 3. ダメージ数字表示（追加機能）
- ヒット時にダメージ数字がポップアップ → フロート → フェードアウト
- クリティカル時は大きい赤文字
- TextMeshPro + DOTween（またはコルーチン）でアニメーション

### 4. キルログ（追加機能）
- 画面右上にキルログ表示（「PlayerA が PlayerB を撃破」）
- 最大5件表示、古い順にフェードアウト

## Unity手動設定 🔧
- Canvas オブジェクトをシーンに配置
- 各Panel/Image/Text を上記構成で作成
- TextMeshPro パッケージ確認（Package Manager）
- 各HUDスクリプトのSerializeFieldにUI要素を手動アサイン
- ミニマップ用カメラ + RenderTexture 作成

## 注意事項
- OnGUI のコードは一旦コメントアウト（フォールバック用に残す）
- uGUI参照がnullの場合はOnGUIにフォールバック（移行中の安全策）

git commit -m "M6-4: UI改善（uGUI移行）"
